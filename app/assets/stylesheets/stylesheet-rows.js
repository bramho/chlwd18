/**
 * This stylesheet holds all styles for rows and ListViews
 */
import {StyleSheet} from "react-native";

 export default RowStyles = StyleSheet.create({
    basicMenuStyles: {
       height: 60,
       borderTopWidth: 1,
    }
 });
